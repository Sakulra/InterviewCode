# 消除相邻近似相等字符

给你一个下标从 `0` 开始的字符串 `word`,它只包含小写英文字母。
一次操作中，你可以选择 word 中任意一个下标 `i`，将 `word[i]` 修改成任意一个小写英文字母。

请你返回消除 word 中所有相邻 近似相等 字符的 最少 操作次数。

对于两个字符a和b，如果a==b或者a和b在字母表中是相邻的，那我们称他们近似相等。

示例1
>输入：word = "aaaaa"  
>输出：2  
>解释：将word变成"azaza",消除所有相邻近似符最少需要两次操作  

示例2
>输入：word = "abddez"  
>输出：2  
>解释：将word变为"zbdzez"即可，只需要两步

示例3
>输入：word = "zyxyxyz"  
>输出：3  
>解释：将word变为"zaxaxaz"即可，需要三步