# 计算最小成本路径

给定一个由随机数组成的正方形二维数组请计算从左上角移动到给定坐标所需的最小成本路径，只能向下或向右移动，包括其实和结束位置。

注意：
·输入参数为：给定的正方形二维数组，以及需要到达的坐标
·输出参数为：最小成本路径的值
·坐标在水平方向记为x在垂直方向记为y

示例
>输入：
grid =[
    [1,2,3],
    [4,8,2],
    [1,5,3]];
x = 2, y = 2
>输出：11